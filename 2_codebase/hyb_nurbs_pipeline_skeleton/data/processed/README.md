# Processed Data

`data/processed/` stores generated datasets such as ML `.npz` files and feature indexes. These files should be reproducible from `data/raw/`, `configs/`, and `outputs/`, so generated tables and model-ready arrays are ignored by git.
