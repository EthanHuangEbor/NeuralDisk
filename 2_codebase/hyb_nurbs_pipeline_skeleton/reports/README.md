# Reports

Use `reports/` for curated, human-facing artifacts selected from generated outputs:

```text
reports/
  figures/
  tables/
```

Keep raw exports in `data/raw/` and reproducible run artifacts in `outputs/`. Do not store private forms or identity documents here.
